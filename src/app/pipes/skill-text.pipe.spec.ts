import { SkillTextPipe } from './skill-text.pipe';

describe('SkillTextPipe', () => {
  it('create an instance', () => {
    const pipe = new SkillTextPipe();
    expect(pipe).toBeTruthy();
  });
});
