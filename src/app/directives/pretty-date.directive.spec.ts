import { PrettyDateDirective } from './pretty-date.directive';

describe('PrettyDateDirective', () => {
  it('should create an instance', () => {
    const directive = new PrettyDateDirective();
    expect(directive).toBeTruthy();
  });
});
